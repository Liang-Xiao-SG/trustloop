import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack screenOptions={{ headerShown: false }}>
      {/* Default stack navigation for welcome, signin, signup */}
    </Stack>
  );
}
